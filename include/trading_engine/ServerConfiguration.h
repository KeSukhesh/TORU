#pragma once

class ServerConfiguration {
public:
    ServerConfiguration();
    // Placeholder for configuration properties
    // coming soon (the plan is to inject a json file into here but we'll see)
};